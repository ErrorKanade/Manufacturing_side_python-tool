📊 PDCA 測試項批量對比工具 (v4.4)本項目是一款專為製造與硬體測試工程（PDCA / 產線測試）設計的 測試指標（Test Items）與上下限（Limits）批量對比工具。工具採用 Python 自動化腳本，能快速掃描多個站位的舊版（Old）與新版（New）測試配置 CSV 檔案，比對出完全一致、上下限變更、新增項目、刪除項目及名稱微調等差異，並自動生成一份帶有可點擊跳轉超連結、色彩高亮標記的 Excel 高級可視化彙總報告（Dashboard）。

🚀 核心特性自定義 CSV 結構精準截取：自動定位 CSV 檔案中的特徵錨點標籤 All Parametric Keys。精準跳過無關信息，動態提取 項目名稱（Keys）、下限（Lower Limits） 以及 上限（Higher Limits）。
自動處理重名測試項（重複 Key），通過內置計數器生成 Unique_Key 避免數據覆蓋。
雙層智能比對算法：第一層（精確匹配）：快速鎖定新舊版本中完全相同的測試項，並實時核對上下限。
若上下限發生變動，會精準標記出具體是上限還是下限被修改。
第二層（模糊匹配）：針對產線工程師微調測試項名稱的情況，引入 fuzzywuzzy 的 Levenshtein 距離算法。當名稱相似度 $\ge 90\%$ 時，自動識別為「名稱微調」，防止誤判為刪除與新增。
交互式級聯 Excel 報表生成：Dashboard 彙總看板：一鍵總覽所有站位的測試項變更數據（一致/變更/新增/刪除/微調的數量）。
雙向超連結跳轉：匯總表中的站位支持點擊直接跳轉到對應站位的詳細比對 Sheet；而各分頁也內置了 🔙 返回彙總表 的快捷跳轉按鈕。
生產力色彩直觀高亮：
🟡 黃色：項目變更 / 名稱微調。
🔴 紅色背景：新增項目。
🔴 粗體紅字：在變更項目中，精準將發生變動的具體數值單元格標紅。
⚪ 灰色：已刪除項目。
免配置，即拖即用：支持標準控制台交互，用戶只需將總文件夾拖入終端，即可自動完成批量分析與打包。
🛠️ 數據處理與比對流程項目核心的數據提取與多級比對邏輯如下：[原始 CSV 檔案] 
      │
      ▼尋找 "All Parametric Keys" 錨點
[提取: Key / Lower Lim / Higher Lim]
      │
      ├───► 1. 鍵值精確匹配 ──► 檢查上下限 ──► 一致 (無色) 或 變更 (🟡 且數值 🔴)
      │
      └───► 2. 剩餘未匹配項 ──► 模糊比對 (Similarity >= 90%?) 
                                    ├───► YES ──► 名稱微調 (🟡)
                                    └───► NO  ──► 舊項 ──► 項目刪除 (⚪)
                                              └───► 新項 ──► 新增項目 (🔴)
📦 環境依賴在運行本工具之前，請確保環境中已安裝 Python 3.8+ 以及以下依賴庫：數據處理：pandas、numpyExcel 樣式控制：openpyxl模糊文本匹配：fuzzywuzzy 及其加速器 python-Levenshtein（可選，推薦安裝以提升效率）可以使用以下命令快速安裝：Bashpip install pandas openpyxl fuzzywuzzy python-Levenshtein
📂 數據準備與目錄規範為了實現批量全自動比對，請將待比對的文件按照以下結構進行組織。工具支持一個總文件夾下包含多個站位子文件夾的批量模式：Plaintext📁 測試項對比總文件夾 (名字可自定義，直接拖入工具中)
│
├── 📁 站位A_FT1_Test_Station/          # 子文件夾代表一個測試工站
│     ├── 📄 O_FT1_config.csv         # 舊版配置：必須以大寫 'O' 或小寫 'o' 開頭
│     └── 📄 N_FT1_config.csv         # 新版配置：必須以大寫 'N' 或小寫 'n' 開頭
│
├── 📁 站位B_FT2_Test_Station/
│     ├── 📄 O_FT2_v1.csv
│     └── 📄 N_FT2_v2.csv
│
└── 📄 所有站位比對彙總表.xlsx         # 運行成功後，工具會自動在此處生成該報告

💡 數據源 CSV 格式要求CSV 檔案內部必須包含一行名為 All Parametric Keys 的文本行，工具會自動以此行為基準，將其下一行作為 Key，下三行作為 Lower Lim，下四行作為 Higher Lim 進行動態偏移提取。

🏃 使用說明在終端中切換至腳本所在目錄，運行以下命令啟動工具：Bashpython "PDCA 测试项批量对比工具.py"
出現歡迎界面與說明後，將你的「總文件夾」直接拖動到終端窗口中（工具會自動處理可能產生的引號），然後按下 Enter。腳本將依次顯示正在處理的子文件夾名稱。
處理完成後，會在總文件夾下生成一個名為 所有站位比對彙總表.xlsx 的 Excel 檔案。
📊 輸出報告（Excel）樣式指南生成的 Excel 報告包含兩種視圖：

Dashboard彙總：
包含列：statio、Sheet（帶超連結）、一致、變更、新增、刪除、名稱微調。

station詳細比對分頁：採用新舊版本「左右並排對照（VS）」設計。
單元格背景與字體顏色嚴格遵循「黃色變更、紅色新增、灰色刪除」的工程規範，且發生修改的上下限數值會單獨以粗體紅字呈現，一目了然。
